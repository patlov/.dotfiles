---
description: Review blockers, architecture fit, and overengineering in HTML
argument-hint: "[files, branch, PR, feature, or scope]"
---
Use the `architecture-review` skill and follow its complete workflow. Review this scope:

${ARGUMENTS:-Use the skill's default scope selection.}
